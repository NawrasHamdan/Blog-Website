<%- include('partials/header'); -%>

  <% Object.keys(myPosts).forEach(function(prop) { %>
    <div class="card">
      <div class="card-body">
        <div class="card-title">
          <% myPosts.prop[0] %>
        </div>
        <p class="card-text article">
          <% myPosts.prop[1] %>
        </p>
      </div>
    </div>
    }); %>
    <%- include('partials/footer'); -%>